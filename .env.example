# --- Database ---
POSTGRES_USER=orbita
POSTGRES_PASSWORD=change_me
POSTGRES_DB=orbita
POSTGRES_HOST=db
POSTGRES_PORT=5432
DATABASE_URL=postgresql+asyncpg://orbita:change_me@db:5432/orbita

# --- Redis ---
REDIS_URL=redis://redis:6379/0

# --- Copernicus Data Space Ecosystem (OAuth2 client-credentials) ---
# Rotate any credential that was ever pasted into a chat/log before use.
COPERNICUS_CLIENT_ID=sh-3c5bdc71-928a-4f9d-afac-34eeb5a91881
COPERNICUS_CLIENT_SECRET=BxbMHML9brDLv2YlFKEGG4UJL25EefDy
COPERNICUS_TOKEN_URL=https://identity.dataspace.copernicus.eu/auth/realms/CDSE/protocol/openid-connect/token
COPERNICUS_CATALOG_URL=https://catalogue.dataspace.copernicus.eu/stac
COPERNICUS_PROCESS_URL=https://sh.dataspace.copernicus.eu/api/v1/process

# --- Storage ---
DATA_DIR=/data
RAW_DIR=/data/raw
PROCESSED_DIR=/data/processed
TILE_DIR=/data/tiles
INDEX_DIR=/data/indexes
# Stage the real RemoteCLIP checkpoint here once its license is verified
# (docs/models/MODEL_SELECTION.md) — falls back to a loud random-init
# warning if this file doesn't exist. Never a silent fake result.
REMOTE_CLIP_WEIGHTS_PATH=/data/models/remote_clip_vit_b_32.pt

# --- Mode ---
OFFLINE_MODE=false
ENVIRONMENT=development
LOG_LEVEL=INFO
